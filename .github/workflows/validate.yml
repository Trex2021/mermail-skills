name: Validate skills

on:
  pull_request:
  push:
    branches: [main, "proof/pr-124-b9341d8"]
    tags: ["v*"]
  schedule:
    - cron: "17 3 * * *"
  workflow_dispatch:
    inputs:
      live_margin_guard:
        description: "Optional live proof; seed-and-prove sends two synthetic emails back to the same test mailbox"
        required: true
        default: connection_only
        type: choice
        options:
          - connection_only
          - seed_and_prove
          - resume_and_prove
      resume_run_tag:
        description: "Existing run tag for a read-only resume (for example 34372972140-1)"
        required: false
        default: ""
        type: string

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
      - run: npm test
      - name: Validate production MCP contract
        if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' || startsWith(github.ref, 'refs/tags/') || github.ref == 'refs/heads/proof/pr-124-b9341d8'
        env:
          MERMAIL_MCP_TEST_API_KEY: ${{ secrets.MERMAIL_MCP_TEST_API_KEY }}
          MERMAIL_REQUIRE_TEST_API_KEY: ${{ (github.event_name == 'workflow_dispatch' || github.ref == 'refs/heads/proof/pr-124-b9341d8') && '1' || '0' }}
        run: |
          for attempt in 1 2 3 4; do
            if npm run validate:remote; then
              exit 0
            fi
            if [ "$attempt" -lt 4 ]; then
              sleep $((attempt * 5))
            fi
          done
          exit 1
      - name: Run live Margin Guard proof
        if: github.event_name == 'workflow_dispatch' && inputs.live_margin_guard == 'seed_and_prove'
        env:
          MERMAIL_API_KEY: ${{ secrets.MERMAIL_MCP_TEST_API_KEY }}
          MERMAIL_LIVE_SEND_APPROVED: "1"
          MERMAIL_LIVE_RUN_TAG: ${{ github.run_id }}-${{ github.run_attempt }}
        run: npm run prove:live-margin
      - name: Resume live Margin Guard proof without another send
        if: github.event_name == 'workflow_dispatch' && inputs.live_margin_guard == 'resume_and_prove'
        env:
          MERMAIL_API_KEY: ${{ secrets.MERMAIL_MCP_TEST_API_KEY }}
          MERMAIL_LIVE_RESUME: "1"
          MERMAIL_LIVE_RUN_TAG: ${{ inputs.resume_run_tag }}
        run: npm run prove:live-margin
      - name: Prove PR 124 head without another send
        if: github.event_name == 'push' && github.ref == 'refs/heads/proof/pr-124-b9341d8'
        env:
          MERMAIL_API_KEY: ${{ secrets.MERMAIL_MCP_TEST_API_KEY }}
          MERMAIL_LIVE_RESUME: "1"
          MERMAIL_LIVE_RUN_TAG: "34372972140-1"
        run: npm run prove:live-margin
